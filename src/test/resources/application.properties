# Banco H2 em mem\u00F3ria para testes
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Recria o esquema a cada teste
spring.jpa.hibernate.ddl-auto=create-drop

# Desabilita o Security para facilitar (ou configure conforme necess\u00E1rio)
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Token Mock para testes
cosmos.api.token=TEST_TOKEN
cosmos.api.url=http://localhost:8080/mock-cosmos